// Compile-time configuration generated by the Python build system.
// Do not edit — this file is regenerated on each compilation.
// Jinja2 template variables: solidworks_api_path, rot_moniker, cache_buster

namespace CosmonSWService
{
    /// <summary>
    /// Compile-time configuration baked in by Python during service compilation.
    /// Contains the SolidWorks API path and ROT moniker that were detected at
    /// build time, ensuring compile-time references and runtime resolution are
    /// always consistent.
    /// </summary>
    static class CompileTimeConfig
    {
        /// <summary>
        /// Path to the SolidWorks API redist directory (interop DLLs).
        /// Used by the AssemblyResolve handler to locate SolidWorks assemblies at runtime.
        /// </summary>
        public const string SolidWorksApiPath = @"{{ solidworks_api_path }}";

        /// <summary>
        /// ROT moniker identifying the specific SolidWorks process to connect to
        /// (e.g. "SolidWorks_PID_12345").
        /// </summary>
        public const string RotMoniker = "{{ rot_moniker }}";
        // {{ cache_buster }}
    }
}
